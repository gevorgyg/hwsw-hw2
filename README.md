# Ant Colony Simulator

To run:
- `./script.sh --demo` - loop over all four approaches (traditional, faas, traditional-part3, faas-part3) `workloads/demo.txt`
- `./script.sh --measure <approach> <workload>` -  runs perf stat -r 10 for
  chosen approach and workload
